{
  "name": "@dragabyte/landing",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint vite.config.ts playwright.config.ts src --max-warnings 0",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "vite": "^8.3.0"
  }
}
